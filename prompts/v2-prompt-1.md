Make a new version of /v1/ called /v2/

Redo the architecture so it runs on the best for a web browser based studio, with everything in local storage but exportable as .cryptart files to back up individual files, and an integration with Google Drive like being able to send a share link to a video or asset and have it be added to the library of stuff, also being able to make a folder on your computer that it reads from. Make it all work locally via a web browser URL of cryptartist.com/web/mediamogul/

Add everything else in the /v1/ as well as placeholders.

Focus on implementing Media Mogul as well.

It's going to be a web browser based video editor, a lot like capcut and davinci resolve.

It will have AI features, that you have to supply your own OpenAI API Key or OpenRouter Key to. These will also be environment variables that can be accessed by the website for users who enter a specific password assigned seperately to each user.

Suggest 3 different architectures and stop output.